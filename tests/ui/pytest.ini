[pytest]
testpaths = .
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    visual: tests requiring a real display for screenshots
    slow: tests taking more than 10 seconds
    requires_wlrctl: tests requiring wlrctl for pointer simulation
    headless: tests that can run in headless mode
timeout = 30
